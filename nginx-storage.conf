server {
    listen 5001;
    client_max_body_size 500M;
    location /storage/v1/ {
        proxy_pass http://localhost:5000/;
        proxy_set_header Host $host;
    }
    location / {
        proxy_pass http://localhost:5000;
        proxy_set_header Host $host;
    }
}
