version: '3.8'

services:
  db:
    image: postgres:15-bookworm
    container_name: supabase-db
    environment:
      # On met le mot de passe en dur ici pour être sûr que Docker le lise
      POSTGRES_PASSWORD: buyticle
    volumes:
      - supabase_db:/var/lib/postgresql/data
    networks:
      - supabase-network

  rest:
    image: postgrest/postgrest:v12.0.2
    container_name: supabase-rest
    # FORCE L'UTILISATEUR ROOT POUR ÉVITER L'ERREUR ID 1000
    user: "0:0" 
    depends_on:
      - db
    environment:
      # Vérifie bien que l'hôte est "db" (le nom du service plus haut)
      PGRST_DB_URI: postgres://postgres:buyticle@db:5432/postgres
      PGRST_DB_SCHEMAS: public
      PGRST_JWT_SECRET: jksdfhksdfldjflhslfhljskhflsjhgsdjfhgskjg
    networks:
      - supabase-network

  studio:
    image: supabase/studio:latest
    container_name: supabase-studio
    depends_on:
      - rest
    environment:
      SUPABASE_URL: http://rest:3000
      STUDIO_DEFAULT_ORGANIZATION: "Buyticle"
      STUDIO_DEFAULT_PROJECT: "Inventaire"
    networks:
      - supabase-network

networks:
  supabase-network:
    driver: bridge

volumes:
  supabase_db: